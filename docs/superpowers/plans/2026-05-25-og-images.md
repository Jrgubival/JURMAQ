# OG Images Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Generate 2 static OG images (1200×630 PNG, < 300KB c/u) para constructora y barraca, según el spec `2026-05-25-og-images-design.md`.

**Architecture:** Script Node ESM one-off en `scripts/og/generate-og-images.mjs` que usa `puppeteer-core` (ya en deps) + system Chrome local. Construye 2 HTML strings con CSS inline + fotos inlined como base64, screenshot a 1200×630, escribe PNGs.

**Tech Stack:** Node 20+, ESM, puppeteer-core 24.x, Chrome local (`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`)

---

## File Structure

- Create: `scripts/og/generate-og-images.mjs` — script principal
- Create: `apps/constructora/public/og-image-1200x630.png` — output binario
- Create: `apps/barraca/public/og-image-barraca-1200x630.png` — output binario

No se modifican layouts (ya referencian estas rutas). No se agregan deps.

---

### Task 1: Crear script generador

**Files:**
- Create: `scripts/og/generate-og-images.mjs`

- [ ] **Step 1: Crear directorio scripts/og/**

Run: `mkdir -p scripts/og`

- [ ] **Step 2: Escribir el script completo**

Contenido íntegro de `scripts/og/generate-og-images.mjs`:

```javascript
#!/usr/bin/env node
/**
 * Genera 2 OG images estáticas (1200×630 PNG) para constructora y barraca.
 *
 * Uso: node scripts/og/generate-og-images.mjs
 *
 * Requiere: puppeteer-core (en deps), Google Chrome instalado en /Applications.
 *
 * Outputs:
 *   apps/constructora/public/og-image-1200x630.png
 *   apps/barraca/public/og-image-barraca-1200x630.png
 */
import { readFile, writeFile, stat } from 'node:fs/promises';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const ROOT = resolve(__dirname, '../..');

const CHROME_PATH =
  '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome';

const VIEWPORT = { width: 1200, height: 630, deviceScaleFactor: 1 };

const FONT_LINK = `
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Newsreader:wght@500&family=Geist:wght@400;600&display=block" rel="stylesheet">
`;

async function toDataUri(absPath) {
  const buf = await readFile(absPath);
  const ext = absPath.split('.').pop().toLowerCase();
  const mime = ext === 'png' ? 'image/png' : 'image/jpeg';
  return `data:${mime};base64,${buf.toString('base64')}`;
}

function constructoraHtml(heroDataUri) {
  return `<!doctype html>
<html lang="es-CL">
<head>
  <meta charset="utf-8">
  ${FONT_LINK}
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html, body { width: 1200px; height: 630px; overflow: hidden; }
    body {
      position: relative;
      background: #0c1d3a url('${heroDataUri}') center/cover no-repeat;
      font-family: 'Geist', -apple-system, sans-serif;
      color: #fff;
    }
    .overlay {
      position: absolute; inset: 0;
      background: linear-gradient(135deg,
        rgba(12,29,58,0.92) 0%,
        rgba(12,29,58,0.55) 50%,
        rgba(12,29,58,0.20) 100%);
    }
    .frame {
      position: absolute; inset: 0;
      padding: 64px;
      display: flex; flex-direction: column; justify-content: space-between;
    }
    .eyebrow {
      font-size: 13px; font-weight: 600;
      letter-spacing: 0.18em; text-transform: uppercase;
      color: #e6b422;
    }
    .stack { display: flex; flex-direction: column; align-items: flex-start; }
    .headline {
      font-family: 'Newsreader', Georgia, serif;
      font-weight: 500;
      font-size: 72px;
      line-height: 1.02;
      letter-spacing: -0.025em;
      color: #ffffff;
      max-width: 760px;
    }
    .rule { width: 56px; height: 2px; background: #e6b422; margin: 28px 0 18px; }
    .tagline {
      font-size: 19px; font-weight: 400;
      color: rgba(255,255,255,0.88);
      letter-spacing: 0.01em;
    }
  </style>
</head>
<body>
  <div class="overlay"></div>
  <div class="frame">
    <div class="eyebrow">Arriendo Maquinaria · Región del Maule</div>
    <div class="stack">
      <div class="headline">Maquinaria pesada<br>en arriendo.</div>
      <div class="rule"></div>
      <div class="tagline">JURMAQ · Curicó · +25 años en construcción</div>
    </div>
  </div>
</body>
</html>`;
}

function barracaHtml(heroDataUri) {
  return `<!doctype html>
<html lang="es-CL">
<head>
  <meta charset="utf-8">
  ${FONT_LINK}
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html, body { width: 1200px; height: 630px; overflow: hidden; }
    body {
      display: grid;
      grid-template-columns: 600px 600px;
      font-family: 'Geist', -apple-system, sans-serif;
      color: #111111;
    }
    .left {
      background: #F7F6F3;
      padding: 64px;
      display: flex; flex-direction: column; justify-content: space-between;
      border-right: 1px solid #EAEAEA;
    }
    .eyebrow {
      font-size: 13px; font-weight: 600;
      letter-spacing: 0.18em; text-transform: uppercase;
      color: #9F2F2D;
    }
    .stack { display: flex; flex-direction: column; align-items: flex-start; }
    .headline {
      font-family: 'Newsreader', Georgia, serif;
      font-weight: 500;
      font-size: 66px;
      line-height: 1.02;
      letter-spacing: -0.025em;
      color: #111111;
    }
    .rule { width: 56px; height: 2px; background: #9F2F2D; margin: 28px 0 18px; }
    .tagline {
      font-size: 19px; font-weight: 400;
      color: #787774;
      letter-spacing: 0.01em;
    }
    .right {
      background: #FFFFFF;
      padding: 48px;
      display: flex; align-items: center; justify-content: center;
    }
    .right img {
      max-width: 100%; max-height: 100%;
      object-fit: contain;
    }
  </style>
</head>
<body>
  <div class="left">
    <div class="eyebrow">Barraca JURMAQ · +1.600 productos</div>
    <div class="stack">
      <div class="headline">Fierros<br>y materiales.</div>
      <div class="rule"></div>
      <div class="tagline">Curicó · Molina · Región del Maule</div>
    </div>
  </div>
  <div class="right">
    <img src="${heroDataUri}" alt="">
  </div>
</body>
</html>`;
}

async function renderToPng(browser, html, outputPath) {
  const page = await browser.newPage();
  await page.setViewport(VIEWPORT);
  await page.setContent(html, { waitUntil: 'networkidle0', timeout: 30_000 });
  await new Promise((r) => setTimeout(r, 500)); // safety wait for font swap
  const png = await page.screenshot({ type: 'png', omitBackground: false });
  await writeFile(outputPath, png);
  await page.close();
  const size = (await stat(outputPath)).size;
  const warn = size > 300 * 1024 ? '  ⚠ exceeds 300KB' : '';
  console.log(`  ✓ ${outputPath.replace(ROOT + '/', '')}  (${Math.round(size / 1024)} KB)${warn}`);
}

async function main() {
  const puppeteer = (await import('puppeteer-core')).default;

  console.log('Inlining hero assets...');
  const [retroDataUri, barracaHeroDataUri] = await Promise.all([
    toDataUri(resolve(ROOT, 'apps/constructora/public/images/maquinarias/retroexcavadora-hmk-102b.jpg')),
    toDataUri(resolve(ROOT, 'apps/barraca/public/images/barraca-hero.jpg')),
  ]);

  console.log('Launching Chrome...');
  const browser = await puppeteer.launch({
    executablePath: CHROME_PATH,
    headless: true,
    args: ['--no-sandbox', '--disable-setuid-sandbox'],
  });

  try {
    console.log('Rendering OG images...');
    await renderToPng(
      browser,
      constructoraHtml(retroDataUri),
      resolve(ROOT, 'apps/constructora/public/og-image-1200x630.png'),
    );
    await renderToPng(
      browser,
      barracaHtml(barracaHeroDataUri),
      resolve(ROOT, 'apps/barraca/public/og-image-barraca-1200x630.png'),
    );
    console.log('Done.');
  } finally {
    await browser.close();
  }
}

main().catch((err) => {
  console.error('OG generation failed:', err);
  process.exit(1);
});
```

- [ ] **Step 3: Verificar Chrome existe**

Run: `test -x "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" && echo OK || echo MISSING`
Expected: `OK`

---

### Task 2: Ejecutar generador

- [ ] **Step 1: Correr el script**

Run: `node scripts/og/generate-og-images.mjs`
Expected output: 2 líneas `✓ apps/.../og-image-...png  (XXX KB)` sin warnings.

- [ ] **Step 2: Verificar dimensiones**

Run: `file apps/constructora/public/og-image-1200x630.png apps/barraca/public/og-image-barraca-1200x630.png`
Expected: ambas líneas contienen `1200 x 630`.

- [ ] **Step 3: Verificar tamaños < 300KB**

Run: `du -k apps/constructora/public/og-image-1200x630.png apps/barraca/public/og-image-barraca-1200x630.png`
Expected: ambos archivos < 300 KB.

---

### Task 3: Inspección visual

- [ ] **Step 1: Abrir los PNG para inspección humana**

Usar el tool `Read` con cada path para mostrar la imagen visualmente. Verificar:
- Texto legible
- Composición balanceada
- Colores correctos (navy constructora, warm bone + rojo barraca)
- Sin artefactos de render (fonts cargadas, no flash de fallback)

Si algo se ve mal: ajustar el script (font-size, padding, colores), volver a correr Task 2.

---

### Task 4: Validación metadata + commit

- [ ] **Step 1: Confirmar paths matchean los layouts**

Run: `grep -n "og-image" apps/constructora/src/app/layout.tsx apps/barraca/src/app/layout.tsx | head -10`
Expected: paths `/og-image-1200x630.png` y `/og-image-barraca-1200x630.png` referenciados.

- [ ] **Step 2: Commit del script + assets**

Run:
```bash
git add scripts/og/generate-og-images.mjs \
        apps/constructora/public/og-image-1200x630.png \
        apps/barraca/public/og-image-barraca-1200x630.png \
        docs/superpowers/specs/2026-05-25-og-images-design.md \
        docs/superpowers/plans/2026-05-25-og-images.md
git commit -m "$(cat <<'EOF'
feat(seo): add OG images 1200x630 for constructora and barraca

Genera los assets que ya estaban referenciados en openGraph.images de
ambos layouts pero faltaban en /public. Editorial premium con foto
hero + overlay navy (constructora) y split 50/50 cream+producto
(barraca). Script reusable en scripts/og/ para regenerar.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 3: Sugerir validación remota al user**

Mensaje final al user: una vez deployed, validar con `https://www.opengraph.xyz/url/https%3A%2F%2Fjurmaq.cl` y equivalente para `barraca.jurmaq.cl`. Localmente solo se puede ver el HTML rendered, no la preview real del crawler.

---

## Notas

- **No TDD aquí**: el deliverable es 2 binarios estáticos. La verificación es visual + metadata (file/du). Agregar tests automatizados para un one-off es over-engineering.
- **No tocar layouts**: ya referencian las rutas correctas. Solo hace falta que los archivos existan.
- **Script reusable**: si en el futuro hay que regenerar (cambio de marca, nuevo headline), `node scripts/og/generate-og-images.mjs` lo hace en ~5s.
